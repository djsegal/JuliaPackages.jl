emacsvterm integration julia repl emacs vterm terminal installation prompt type add following config startup atreplinit eval using optionally set options configure backend putting features jump prompts buffers informs directory changing file starts documentation doc invocation mode enabled shown separate buffer disable functionality run markdown false happy chooses display via action example piece code init ensures reused otherwise window appropriate width created list alist reuse images img default enable image true addition based multimedia disabled resp