showoff provides interface consistently formatting array dates unitful values gadfly label axes keys defines function called takes abstractarray type returns strings length look nice plotted define example using struct percent value float return string importantly print percentages map println rand notice compared digits trailing aligned specialized defined falls package originally written daniel jones