betweenflags set string processing utility functions removes text flags documentation build status installation install julia add featured greedy similar regex pattern matching useful strings example remove comments comment instance closing recognized flat start vector stop examples using grabbed offers simple interface print note effectively replaceable provide nice level based version finding function confused statement inside corresponding zero myfunc stuff else word boundaries left outer flagset flag inner results complex