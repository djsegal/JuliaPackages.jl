weighted arrays simple package defines weightedmatrix struct vector weights corresponding columns matrix default add array values box constraint julia randn float unclamped normalised element rand ones clamped examples roughly wrandn wrand sub random sobol regular wgrid mutable clamp enforce normalise subtypes abstractarray functions instance rows hcat concatenate mapslices act restore sort arranges sortcols instead unique accumulate identical log tanh wise update constraints appropriately dimensional belong dimension plot recipes defined indicating weight example shown grid plus bivariate normal distribution using plots soboln diamond red principal components attempt scale correctly function pplot saves pca help wpca global variable axes