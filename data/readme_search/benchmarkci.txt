benchmarkci provides easy run benchmark suite via github actions minimal setup declared benchmarktools pkgbenchmark api warning package experimental fix version requires defining information julia project toml running create workflow file required workflows yml following configuration name benchmarks pull request jobs runs ubuntu steps checkout buildpkg install dependencies using pkg add judge post results postjudge env token secrets don code push conditionally trigger label types labeled synchronize reopened job contains event labels manifest checked repository include parent dev directory shell myproject activate activating environment additional recommended lines gitignore json useful locally printing result optional posting comment noisy displayjudgement instead print judgement store git branch alternatively report markdown pushed example pushresult ssh key documenter method travis storage format changed releases interactively