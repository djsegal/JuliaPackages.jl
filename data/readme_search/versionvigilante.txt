versionvigilante enforces rule pull requests increase version julia package project toml file helps automate continuous delivery process description workflow packages available basic usage main using github actions add following repo named workflows request yml name jobs runs ubuntu steps checkout setup run pkg repository labeller success outputs compare versions failure continue error true script token secrets issues removelabel context issue bump addlabels labels travis replace myusername mypackage correct values include stage type branch master set bors click instructions