overview looking simple run binaries yggdrasil repo runbinary covered hood performs following steps activate temporary environment temp add required jll package env import target executable usage julia exports single macro using basic binary specified suffix example sqlcipher imagemagick identify omit name contains passes command line arguments version started current directory default set activated desired tempdir true separate scripts provided executed via cli reasonably convenient