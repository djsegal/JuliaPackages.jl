tasklocalvalues task local storage julia allows developers store data solves accesses type stable key conflict users common pattern currently symbols keys packages symbol silently object identity value gurantueeing silent conflicts occur tasklocalvalue struct carries initializer information usage import base threads spawn using const counter int initialized sync begin tasks copy