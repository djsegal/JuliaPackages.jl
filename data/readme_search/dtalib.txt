package provides support stata dta files verison quickstart using pkg add dtalib data read dataset function returns structure includes fields header continuing previous example separately accessed variable dot syntax similarly features currently supports reading specifically versions variables information including label timestamp missing values plans writing strings bytes aka strl terms labels labelling binary categorical encoded