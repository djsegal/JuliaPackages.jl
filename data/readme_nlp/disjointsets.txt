disjointset implementation disjointset data structure julia package based juliacollections implementation disjointset data structure datastructuresjl package unionsize unionrank allows size set nearconstant time usage intdisjointset creates forest comprised singletons union merges sets contain returns root set rootunion merges sets root returns root set assumes findroot root element subset contains insameset determines set numsets returns sets setsize size set elem push adds single element set returns element operation called makeset element types disjointset abstractstring union insameset push etc