ifelsejl sometimes function form conditional julia base defines ifelse psyche defined base core rarely matters re trying define dispatch coreifelse error message julia base ifelse array error add methods builtin function stacktrace top level scope none methods added function current form blocking symbolic libraries modelingtoolkitjl vectorization libraries loopvectorizationjl extending operations function onto types issue separate version ifelse extendable difficult standard inference issues pr meantime allow development ifelse dispatches common platform ifelsejl created hold common definition ifelsejl defines ifelseifelse call coreifelse fallbacks free extend libraries extend ifelse function common form fixed function deprecated extendable baseifelse allow thriving package ecosystem continue develop