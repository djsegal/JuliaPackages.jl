spatialgridsjl spatialgrids provides d d grid structures cloud data current grids available d raster sparse voxel grid d cloud raster grid create d raster grid using spatialgrids rand raster rasterizepoints sparse voxel grid create sparse voxel grid voxel length metres arbitrary using spatialgrids rand grid sparsevoxelgrid created grid iteratable object returns voxel index voxel accessed directly loop voxels collect ed array likewise returned voxel iterable object returns indices iterate voxel grid voxel grid index voxel idx voxel stuff idx indices voxel allpointindices collect voxel