binstatisticsjl highly flexible efficient computation dimensional binned statistic variable binstatistics provides binstats function build top dataframesjl catagoricalarraysjl binstats xx faster python scipy binstats function binstatsdf axiscol axisedges bincol grpfunction nrow colfunction mean missingbin false returns dataframe containing function values binned variables df arguments axiscol binning axes column axisedges bin edges axiscol bincol column variable binned grpfunction nrow column independent funciton applied level varfunction mean column dependent funciton applied bincol level missingbin false include missing bins examples load packages using pkg pkg add binstatistics pkg add dataframes pkg add statistics pkg add cairomakie using binstatistics using dataframes using statistics using cairomakie synthetic data begin df dataframe df rand df rand df v cos df randn df v cos df df sin df df randn df v df v df v example calculate countnrow mean v binned according df binstats df v dataframe row nrow vmean float int float note labels bin centers example calculate countnrow mean v v binned according df binstats df v v dataframe row nrow vmean vmean float int float float example calculate countnrow mean medain std v binned according df binstats df v colfunction mean median std dataframe row nrow vmean vmedian vstd float int float float float example calculate countnrow mean v binned according df binstats df v missingbins true dataframe row nrow vmean float float int float example calculate median v binned according using uniform axisedges df binstats df v grpfunction colfunction median missingbins true dataframe row vmedian float float float example apply custom function v binned according create median absolute deviation function function mad median abs median binstats accepts anonymous functions output assinged generic name apply data df binstats df v grpfunction colfunction mad missingbins true dataframe row vmad float float float plotting script example begin fig figure axis fig title raw data scatter fig df df v axis fig title binned data scatter fig df df vmean fig example begin fig figure axis fig title raw data scatter fig df df v scatter fig df df v axis fig title binned data scatter fig df df vmean label v scatter fig df df vmean label v axislegend fig example begin fig figure axis fig title raw data scatter fig df df v axis fig title binned data scatter fig df df vmean label mean scatter fig df df vmedian label median scatter fig df df vstd label std axislegend fig example begin fig figure axis fig title raw data scatter fig df df color df v colormap thermal markersize xlims ylims axis fig title binned data heatmap fig unique df unique df reshape df vmean length unique df length unique df colormap thermal fig example begin fig figure axis fig title raw data scatter fig df df color df v colormap thermal markersize xlims ylims axis fig title binned data heatmap fig unique df unique df reshape df vmedian length unique df length unique df colormap thermal fig example begin fig figure axis fig title raw data scatter fig df df color df v colormap thermal markersize xlims ylims axis fig title binned data heatmap fig unique df unique df reshape df vmad length unique df length unique df colormap thermal fig similar packages julia binnedstatisticsjl single variable binned statistics python scipy binnedstatistic binnedstatisticd binnedstatisticdd single variable dimensional binned statist